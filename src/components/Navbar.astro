---
const links = [
  { name: "Home", href: "#home" },
  { name: "Info Terkait", href: "#info" },
  { name: "Statistik", href: "#statistik" },
  { name: "Tips Kesehatan", href: "#tips" },
  { name: "About Us", href: "#about" },
];
---

<nav class="fixed top-0 left-0 w-full bg-white/80 backdrop-blur shadow z-50">
  <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8">
    <div class="flex justify-between items-center h-16">
      <!-- Logo -->
      <a href="#home" class="text-xl font-bold text-blue-600">🌱 Natriplast</a>

      <!-- Links -->
      <ul class="flex space-x-6">
        {links.map(link => (
          <li>
            <a
              href={link.href}
              class="nav-link relative text-blue-700 hover:text-purple-600 transition-colors duration-300"
            >
              {link.name}
            </a>
          </li>
        ))}
      </ul>
    </div>
  </div>
</nav>

<style>
  .nav-link::after {
    content: "";
    position: absolute;
    left: 0;
    bottom: -2px;
    width: 0%;
    height: 2px;
    background: #1e3a8a; /* biru */
    transition: all 0.5s ease;
  }
  .nav-link:hover::after {
    width: 100%;
    background: #7c3aed; /* ungu */
  }
</style>
